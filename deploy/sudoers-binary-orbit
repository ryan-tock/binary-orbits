# Lets the auto-deploy oneshot (run as user ryan) bounce the app services
# without a password. Install at /etc/sudoers.d/binary-orbit with mode 440.
ryan ALL=(root) NOPASSWD: /bin/systemctl restart binary-orbit-main.service, /bin/systemctl restart binary-orbit-staging.service
